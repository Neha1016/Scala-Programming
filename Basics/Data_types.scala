object Main {
    def main(args:Array[String]): Unit = {
        val name : String = "Om"
        var age : Int = 44
        var percentage : Float = 88.8f
        var student : Boolean = true 
        var grade : Char = 'A'

        println(name)
        println(age)
        println(percentage)
        println(student)
        println(grade)
    }
}